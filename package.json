{
  "name": "doxan-api",
  "version": "1.0.0",
  "private": true,
  "description": "Doxan — API Node + Supabase (backend). Le front est dans /mobile (Expo).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
